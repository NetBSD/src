#!/bin/sh
#
# $NetBSD: nfsd,v 1.5 2004/08/13 18:08:03 mycroft Exp $
#

# PROVIDE: nfsd
# REQUIRE: mountd

$_rc_subr_loaded . /etc/rc.subr

name="nfsd"
rcvar="nfs_server"
command="/usr/sbin/${name}"
required_vars="mountd rpcbind"

load_rc_config $name
run_rc_command "$1"
