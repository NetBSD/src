#!/bin/sh
#
# $NetBSD: gated,v 1.3.4.1 2000/10/02 01:08:54 lukem Exp $
#

# PROVIDE: gated
# REQUIRE: DAEMON

. /etc/rc.subr

name="gated"
rcvar=$name
command="/usr/sbin/${name}"
pidfile="/var/run/${name}.pid"
required_files="/etc/${name}.conf"

load_rc_config $name
run_rc_command "$1"
