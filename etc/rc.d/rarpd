#!/bin/sh
#
# $NetBSD: rarpd,v 1.3.4.1 2000/10/02 01:08:56 lukem Exp $
#

# PROVIDE: rarpd
# REQUIRE: DAEMON

. /etc/rc.subr

name="rarpd"
rcvar=$name
command="/usr/sbin/${name}"
pidfile="/var/run/${name}.pid"
required_files="/etc/ethers"

load_rc_config $name
run_rc_command "$1"
