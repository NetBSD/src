#!/bin/sh
#
# $NetBSD: rpcbind,v 1.3.4.2 2000/06/23 15:46:03 minoura Exp $
#

# PROVIDE: rpcbind
# REQUIRE: network ntpdate syslogd named ppp

. /etc/rc.subr

name="rpcbind"
command="/usr/sbin/${name}"
pidfile="/var/run/${name}.pid"

load_rc_config $name
run_rc_command "$1"
