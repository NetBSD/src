new()
	allocate する
	基本的な初期化。
	新規にallocする内部変数名は new
init()
	正しい姿になるための初期化。
	ほとんど new() と対
del()
	free()
	list に注意！
rem()
	list から切り離す
ins()
	list へ追加。
	新規にinsertする内部変数名は new
	もし親がいれば head

void()
	return はどうする？


YIPSDEBUG と plog は改行する。

できるだけ K&R形式

2つ併せて評価しないといけない文の時に || でつなげてる所があるかも…
	if (hogea() || hogeb())
		goto error;

ログの統一(案)
phase1
phase2
IV
isakmp
ipsec
ISAKMP-SA
IPsec-SA
