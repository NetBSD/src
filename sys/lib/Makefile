#	$NetBSD: Makefile,v 1.6 1995/06/30 03:00:33 christos Exp $

.if exists(libsa)
SUBDIR+= libsa
.endif
.if exists(libc_sa)
SUBDIR+= libc_sa
.endif
.if exists(libnetboot)
SUBDIR+= libnetboot
.endif

.include <bsd.subdir.mk>
