#	$NetBSD: Makefile.rumpdev,v 1.1.2.4 2009/09/16 13:38:03 yamt Exp $
#

#RUMPDEVLIST=	raidframe
RUMPDEVLIST=	cgd disk netsmb rnd

.for var in ${RUMPDEVLIST}
RUMPDEVLIBS+=lib${var}
RUMPDEVLDADD+=-lrumpdev_${var}
.endfor
