#	$NetBSD: Makefile,v 1.2.12.1 2018/04/16 02:00:08 pgoyette Exp $

.include <bsd.own.mk>

SUBDIR+=	hello
SUBDIR+=	executor
#SUBDIR+=	luahello	# Nothing to build here, only text files
SUBDIR+=	luareadhappy	# Needs an additional Lua script
SUBDIR+=	ping		# Needs an additional helper program
SUBDIR+=	properties
SUBDIR+=	readhappy
SUBDIR+=	sysctl

.include <bsd.subdir.mk>
