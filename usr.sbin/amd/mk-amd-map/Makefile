#	%W% (Berkeley) %G%

PROG=	mk-amd-map
NOMAN=	true
CFLAGS+=-I${.CURDIR}/../config
CFLAGS+=-I${.CURDIR}/../include
CFLAGS+=-DOS_HDR=\"os-netbsd.h\"

.include "../../Makefile.inc"
.include <bsd.prog.mk>
