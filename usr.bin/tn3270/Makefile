#	$NetBSD: Makefile,v 1.6 1997/01/09 20:21:41 tls Exp $

.if make(clean) || make(cleandir) || make(depend) || make(obj) || make(lint)
SUBDIR += tools
.endif
SUBDIR += tn3270 mset

.include <bsd.subdir.mk>
