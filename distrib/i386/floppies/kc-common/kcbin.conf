#
# kcbin.conf - unified binary for the kc floppy
# $Id: kcbin.conf,v 1.3 1995/05/12 03:33:22 cgd Exp $	
#

srcdirs ../../../../bin ../../../../sbin

progs cp dd disklabel fsck init mount reboot sh test umount

ln test [
ln reboot halt
ln sh -sh		# init invokes the shell this way

libs -ledit -lutil -ltermcap -lcrypt -ll
