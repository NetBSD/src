# $NetBSD: 99-print-sysinst,v 1.1.2.2 2009/05/13 19:17:41 jym Exp $

case "${reason}" in
BOUND|INFORM|REBIND|REBOOT|RENEW|TIMEOUT)
	cat > /tmp/dhcpcd-lease << EOF
host-name=${new_host_name}
domain-name=${new_domain_name}
EOF
	set > /tmp/dhcpcd-lease-all
	;;
esac
