/*
 * This file was generated by the mkinit program.
 */

#include "shell.h"
#include "mystring.h"
#include "init.h"
#include "eval.h"
#include <stdio.h>
#include "input.h"
#include "error.h"
#include <stdlib.h>
#include "options.h"
#include "redir.h"
#include <signal.h>
#include "trap.h"
#include "output.h"
#include "memalloc.h"
#include "var.h"



#define ATABSIZE 39
#undef  ATABSIZE
#define ATABSIZE 39
#define MAXPWD 256
#undef  MAXPWD
#define MAXPWD 256
#define main echocmd
#undef  main
#define main echocmd
#define ALL (E_OPEN|E_CREAT|E_EXEC)
#undef  ALL
#define ALL (E_OPEN|E_CREAT|E_EXEC)
#define EV_EXIT 01		/* exit after evaluating tree */
#undef  EV_EXIT
#define EV_EXIT 01		/* exit after evaluating tree */
#define EV_TESTED 02		/* exit status is checked; ignore -e flag */
#undef  EV_TESTED
#define EV_TESTED 02		/* exit status is checked; ignore -e flag */
#define EV_BACKCMD 04		/* command executing within back quotes */
#undef  EV_BACKCMD
#define EV_BACKCMD 04		/* command executing within back quotes */
#define CMDTABLESIZE 31		/* should be prime */
#undef  CMDTABLESIZE
#define CMDTABLESIZE 31		/* should be prime */
#define ARB 1			/* actual size determined at run time */
#undef  ARB
#define ARB 1			/* actual size determined at run time */
#define NEWARGS 5
#undef  NEWARGS
#define NEWARGS 5
#define MAXHISTLOOPS	4	/* max recursions through fc */
#undef  MAXHISTLOOPS
#define MAXHISTLOOPS	4	/* max recursions through fc */
#define DEFEDITOR	"ed"	/* default editor *should* be $EDITOR */
#undef  DEFEDITOR
#define DEFEDITOR	"ed"	/* default editor *should* be $EDITOR */
#define editing (Eflag || Vflag)
#undef  editing
#define editing (Eflag || Vflag)
#define EOF_NLEFT -99		/* value of parsenleft when EOF pushed back */
#undef  EOF_NLEFT
#define EOF_NLEFT -99		/* value of parsenleft when EOF pushed back */
#define MAXCMDTEXT	200
#undef  MAXCMDTEXT
#define MAXCMDTEXT	200
#define MAXMBOXES 10
#undef  MAXMBOXES
#define MAXMBOXES 10
#define PROFILE 0
#undef  PROFILE
#define PROFILE 0
#define SIGSSIZE (sizeof(sigs)/sizeof(sigs[0]))
#undef  SIGSSIZE
#define SIGSSIZE (sizeof(sigs)/sizeof(sigs[0]))
#define MINSIZE 504		/* minimum size of a block */
#undef  MINSIZE
#define MINSIZE 504		/* minimum size of a block */
#define DEFINE_OPTIONS
#undef  DEFINE_OPTIONS
#define DEFINE_OPTIONS
#define EOFMARKLEN 79
#undef  EOFMARKLEN
#define EOFMARKLEN 79
#define EMPTY -2		/* marks an unused slot in redirtab */
#undef  EMPTY
#define EMPTY -2		/* marks an unused slot in redirtab */
#define S_DFL 1			/* default signal handling (SIG_DFL) */
#undef  S_DFL
#define S_DFL 1			/* default signal handling (SIG_DFL) */
#define S_CATCH 2		/* signal is caught */
#undef  S_CATCH
#define S_CATCH 2		/* signal is caught */
#define S_IGN 3			/* signal is ignored (SIG_IGN) */
#undef  S_IGN
#define S_IGN 3			/* signal is ignored (SIG_IGN) */
#define S_HARD_IGN 4		/* signal is ignored permenantly */
#undef  S_HARD_IGN
#define S_HARD_IGN 4		/* signal is ignored permenantly */
#define S_RESET 5		/* temporary - to reset a hard ignored sig */
#undef  S_RESET
#define S_RESET 5		/* temporary - to reset a hard ignored sig */
#define OUTBUFSIZ BUFSIZ
#undef  OUTBUFSIZ
#define OUTBUFSIZ BUFSIZ
#define BLOCK_OUT -2		/* output to a fixed block of memory */
#undef  BLOCK_OUT
#define BLOCK_OUT -2		/* output to a fixed block of memory */
#define MEM_OUT -3		/* output to dynamically allocated memory */
#undef  MEM_OUT
#define MEM_OUT -3		/* output to dynamically allocated memory */
#define OUTPUT_ERR 01		/* error occurred on output */
#undef  OUTPUT_ERR
#define OUTPUT_ERR 01		/* error occurred on output */
#define TEMPSIZE 24
#undef  TEMPSIZE
#define TEMPSIZE 24
#define HAVE_VASPRINTF 1
#undef  HAVE_VASPRINTF
#define HAVE_VASPRINTF 1
#define VTABSIZE 39
#undef  VTABSIZE
#define VTABSIZE 39



extern void rmaliases __P((void));

extern int loopnest;		/* current loop nesting level */

extern void deletefuncs __P((void));

struct strpush {
	struct strpush *prev;	/* preceding string on stack */
	char *prevstring;
	int prevnleft;
	int prevlleft;
	struct alias *ap;	/* if push was associated with an alias */
};

struct parsefile {
	struct parsefile *prev;	/* preceding file on stack */
	int linno;		/* current line */
	int fd;			/* file descriptor (or -1 if string) */
	int nleft;		/* number of chars left in this line */
	int lleft;		/* number of chars left in this buffer */
	char *nextc;		/* next char in buffer */
	char *buf;		/* input buffer */
	struct strpush *strpush; /* for pushing strings at this level */
	struct strpush basestrpush; /* so pushing one is fast */
};

extern int parselleft;		/* copy of parsefile->lleft */
extern struct parsefile basepf;	/* top level input file */
extern char basebuf[BUFSIZ];	/* buffer for top level input file */

extern short backgndpid;	/* pid of last background process */
extern int jobctl;

extern int tokpushback;		/* last token pushed back */
extern int checkkwd;            /* 1 == check for kwds, 2 == also eat newlines */

struct redirtab {
	struct redirtab *next;
	short renamed[10];
};

extern struct redirtab *redirlist;

extern char sigmode[NSIG];	/* current value of signal */

extern char **environ;



/*
 * Initialization code.
 */

void
init() {

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/input.c: */
      {
	      basepf.nextc = basepf.buf = basebuf;
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/var.c: */
      {
	      char **envp;

	      initvar();
	      for (envp = environ ; *envp ; envp++) {
		      if (strchr(*envp, '=')) {
			      setvareq(*envp, VEXPORT|VTEXTFIXED);
		      }
	      }
      }
}



/*
 * This routine is called when an error or an interrupt occurs in an
 * interactive shell and control is returned to the main command loop.
 */

void
reset() {

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/eval.c: */
      {
	      evalskip = 0;
	      loopnest = 0;
	      funcnest = 0;
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/input.c: */
      {
	      if (exception != EXSHELLPROC)
		      parselleft = parsenleft = 0;	/* clear input buffer */
	      popallfiles();
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/parser.c: */
      {
	      tokpushback = 0;
	      checkkwd = 0;
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/redir.c: */
      {
	      while (redirlist)
		      popredir();
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/output.c: */
      {
	      out1 = &output;
	      out2 = &errout;
	      if (memout.buf != NULL) {
		      ckfree(memout.buf);
		      memout.buf = NULL;
	      }
      }
}



/*
 * This routine is called to initialize the shell to run a shell procedure.
 */

void
initshellproc() {

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/alias.c: */
      {
	      rmaliases();
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/eval.c: */
      {
	      exitstatus = 0;
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/exec.c: */
      {
	      deletefuncs();
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/input.c: */
      {
	      popallfiles();
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/jobs.c: */
      {
	      backgndpid = -1;
#if JOBS
	      jobctl = 0;
#endif
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/options.c: */
      {
	      int i;

	      for (i = 0; i < NOPTS; i++)
		      optlist[i].val = 0;
	      optschanged();

      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/redir.c: */
      {
	      clearredir();
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/trap.c: */
      {
	      char *sm;

	      clear_traps();
	      for (sm = sigmode ; sm < sigmode + NSIG ; sm++) {
		      if (*sm == S_IGN)
			      *sm = S_HARD_IGN;
	      }
      }

      /* from /usr/sources/cvs.netbsd.org/src/distrib/arm32/ramdisk/../../../bin/sh/var.c: */
      {
	      shprocvar();
      }
}
